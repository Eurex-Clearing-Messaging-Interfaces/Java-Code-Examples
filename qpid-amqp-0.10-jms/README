HOW TO EXECUTE EXAMPLES:

1) Edit example.properties to suit your needs, file is located at directory src/main/resources.

  a) How to change connection string and queue names is described in documentation.

2) Configure certificates to maintain secure connection to broker

  a) Set passwords to trust store and key store in connection string in file example.properties (default password is '123456')

  b) Set full path to trust store and key store in connection string in file example.properties (default values 'truststore.jks' and 'ABCFR_ABCFRALMMACC1.keystore' will NOT work)

3) Compile and execute

  a) Execute commands 'mvn package' and 'java -jar target/fixml/fixml/lib/fixml-broadcast-receiver.jar' for broadcast receiver example.

  b) Execute commands 'mvn package' and 'java -jar target/fixml/fixml/lib/fixml-request-response.jar' for request response example.

4) Stop example

  a) Any running example can be stopped by pressing Ctrl + C
